# 🎯 AI-DRAKON — Master Context

Останнє оновлення: 2026-06-27 01:10 (EEST)

## 📦 Проект
- **Що це:** Платформа для розробки ПЗ на основі візуальних схем ДРАКОН з інтеграцією ШІ-агентів. Користувач описує алгоритм природною мовою → LLM генерує ДРАКОН-схему (JSON) → схема редагується у графічному редакторі та компілюється в робочий код.
- **Мета:** Створити інтегроване середовище, де ШІ допомагає перетворювати вимоги у візуальні алгоритми, які компілюються у браузері (JS/Lua) та надійно зберігаються у GitHub репозиторії користувача.
- **Стек:** 
  * **Frontend:** React, Vite, TypeScript, Tailwind CSS, TanStack Router (деплой на Cloudflare Pages з `.lovable/src/`).
  * **Gateway:** Cloudflare Worker (`drakon-antigravity-worker`) для авторизації та проксіювання запитів.
  * **Backend & Auth:** Appwrite Cloud (база даних, сесії користувачів).
  * **Агенти (Appwrite Functions):** Docs-Agent (аналіз вимог/docs), Architect-Agent (проектування solution.json), drakon-codegen (генерація схем).
  * **Рантайм & Код:** FastAPI мікросервіси на сервері розробки (порти 8765, 8766, 8767), клієнтські збірки `drakontechgen.js` та `drakongen.js`.
  * **Сховище:** MinIO S3 (бакети для діаграм та логів).

---

## 🏗 Архітектурні рішення

| Рішення | Чому прийнято | Альтернативи |
|---|---|---|
| **Appwrite Student Plan** | Надає готові інструменти Auth, DB та Serverless функції для запуску ШІ-агентів (Docs/Architect). | Традиційні хмарні функції (AWS Lambda), які складніше адмініструвати та конфігурувати. |
| **Cloudflare Workers** | Забезпечує миттєву глобальну маршрутизацію, підпис S3-запитів до MinIO та обхід 15-хвилинного ліміту Appwrite JWT. | Маршрутизація всередині функцій Appwrite, що збільшило б затримку через cold starts. |
| **GitNexus** | Використовується як семантичний граф коду для розрахунку blast radius змін перед редагуванням та аналізу зв'язків. | Стандартний текстовий grep, який не має контексту структури коду та викликів функцій. |
| **MemPalace** | Семантична пам'ять (векторна база ChromaDB) для збереження логів сесій, щоденників (diaries) та контексту розробки. | Реляційні бази даних (Postgres/S3), які не підтримують семантичний пошук та буст валідності запитів. |

---

## 🤖 Флот агентів та активні вузли

> [!IMPORTANT]
> **Поточний режим розробки (червень 2026):**
> Розробка ведеться виключно на трьох вузлах:
> 1. **AGY2** (Windows 11 Laptop, `192.168.3.30`) — кодування через GitHub MCP, ручне та браузерне тестування.
> 2. **rpi3b** (Raspberry Pi 3B Dev Server, `192.168.3.184`) — хостинг GitNexus, сервісів та бази даних.
> 3. **OrangePi PC2** (local session node, `192.168.3.161`) — локальна сесія та оркестрація.
>
> Процес координується силами **двох різних акаунтів AGY** (з різними конфігураціями Git та токенами авторизації).
> **Claude** (як ШІ-оркестратор/агент) наразі не бере участі в активній розробці й буде підключений **лише з наступного місяця**.

Повний опис флоту та планових вузлів:

| Агент | IP | Роль | Статус / Поточна задача |
|---|---|---|---|
| **AGY2** | 192.168.3.30 | Первинний виконавець коду (Windows) | ✅ **Active** (кодування через GitHub MCP, ручне тестування). |
| **Copilot** | 192.168.3.184 | Git/Shell асистент (dev-сервер rpi3b) | ✅ **Active** (керування інфраструктурою, GitNexus). |
| **Claude** | 192.168.3.161 | Оркестратор сесій та планувальник | ⏸️ **Inactive** (буде підключений з наступного місяця). |
| **AGY3** | 192.168.3.204 | Резервний виконавець (планшет) | ⏸️ **Inactive** (тимчасово не використовується). |
| **AGY phone** | 192.168.3.195 | Тестування та швидкі зміни (телефон) | ⏸️ **Inactive** (тимчасово не використовується). |

---

## 🔌 Інфраструктурні MCP-сервери та можливості rpi3b

### 🛠️ Можливості вузла rpi3b (dev-сервер, Copilot):
- **Служби та Бази Даних:** Хостинг векторної БД MemPalace (ChromaDB) та графової Code Intelligence бази GitNexus.
- **Microservices Runtime:** Запуск FastAPI мікросервісів (`drakon-agent` :8765, `architect-agent` :8766, `drakon-codegen` :8767).
- **CI/CD та Git:** Локальна Git-інфраструктура, запуск Docker-контейнерів, виконання фонових скриптів верифікації та тестування.
- **ШІ-сервіси:** Локальні Appwrite Functions з інтеграцією LLM.

### 🔌 Доступні MCP-сервери (Model Context Protocol):
- **GitNexus MCP (`gitnexus`):** Повний аналіз зв'язків у коді, визначення blast radius змін (`impact`), семантичний пошук та виявлення регресій (`detect_changes`).
- **MemPalace MCP (`mempalace`):** Збереження та пошук контексту розробки, ведення та читання щоденників агентів (`diary_write`/`diary_read`), керування фактами.
- **NotebookLM MCP (`notebooklm`):** Створення та аналіз блокнотів, робота з великими обсягами документації, генерація аудіо-подкастів та звітів.
- **n8n MCP (`n8n`):** Дизайн, керування та деплой сценаріїв автоматизації безпосередньо з коду.
- **Agent Workspace MCP (`agent-workspace`):** Автоматизація браузера (PinchTab), створення скріншотів, взаємодія з вікнами та терміналом.
- **OpenDesign MCP (`opendesign`):** Створення проектів та запуск ШІ-генерації UI на основі дизайну.

---

## 📊 Поточний статус
- **Фаза:** Ручне тестування інтерфейсу (сценарії готові).
- **Готовність:** ~85%. Основні сторінки та логіка (Overview, Agents, PlayPipe, Automations, Docs, Settings) інтегровані. Усі FastAPI мікросервіси перенесені в Appwrite Functions.
- **Останній коміт:** `26c36fcc - docs: mark TASK-DRK-21 as complete (Fri Jun 26 21:50:29 2026 +0300)`.
- **Активні гілки:** `main` (Up to date з origin).
- **Тести:** **38 Vitest тестів** пройшли на 100% успішно (green).

---

## 🧪 Протокол тестування (завтра)
1. **Сценарій 1:** Створення проекту та перевірка графа знань (TASK-DRK-21). Перевірка автоматичної ініціалізації та пушу `.understand-anything/knowledge-graph.json` на GitHub.
2. **Сценарій 2:** Робота в Agent Studio, EVIDENCE Panel та Runtime (TASK-DRK-20/22). Перевірка гарячих клавіш Ctrl+S, аналізу впливу в EVIDENCE панелі та експорту EVE ZIP.
3. **Сценарій 3:** Декомпозиція додатку через PlayPipe (UI-3.1/3.2). Перевірка ШІ-генерації графів компонентів та моніторингу статусів збірки через SSE.
4. **Сценарій 4:** Дизайн воркфлоу n8n та Deploy (UI-4.1/4.2). Перевірка інтерактивного налаштування параметрів n8n-вузлів у сидебарі та пушу воркфлоу на сервер.

---

## 🚀 Наступні кроки (пріоритезовані)
1. Провести повне ручне тестування інтерфейсу згідно з плейбуком (`manual-testing-playbook.md`).
2. Додати автоматичну верифікацію наявності графа знань у GitHub репозиторії після завершення New Project Wizard.
3. Покрити інтеграційними тестами n8n-автоматизації та перевірити роботу форми з'єднання з n8n.

---

## ⚠️ Відкриті питання
- Надійність механізму silent-refresh для JWT-сесій Appwrite в умовах нестабільного з'єднання.
- Оптимізація tree-sitter JS/TS парсингу в Appwrite-функціях для запобігання OOM лімітів на низькопродуктивних середовищах.

---

## 🔗 Ключові файли
- **CONTEXT.md:** `/home/vokov/workspace/ai-drakon-scaffolder/CONTEXT.md`
- **Playbook:** `/home/vokov/workspace/ai-drakon-scaffolder/development/plans/2026-06-27-manual-testing-playbook.md`
- **Infra workflows:** `/home/vokov/projects/exodus-infra/workflows/agent-fleet.md` (на dev-сервері)
