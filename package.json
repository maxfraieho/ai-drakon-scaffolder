{
  "name": "workspace-root",
  "private": true,
  "scripts": {
    "sync:deps": "npm --prefix .lovable install --prefer-dedupe",
    "dev": "npm run sync:deps && npm --prefix .lovable run dev --",
    "build": "npm run sync:deps && npm --prefix .lovable run build && npm run prepare:pages-dist && npm run prepare:pages-functions",
    "prepare:pages-dist": "node scripts/prepare-cloudflare-pages.mjs",
    "prepare:pages-functions": "node scripts/prepare-cloudflare-functions.mjs",
    "dist-check": "npm run build",
    "build:dev": "npm run sync:deps && npm --prefix .lovable run build:dev",
    "preview": "npm run sync:deps && npm --prefix .lovable run preview",
    "test": "npm run sync:deps && npm --prefix .lovable exec vitest",
    "build:pages": "npm run build",
    "deploy:pages": "npx wrangler pages deploy dist --functions functions"
  },
  "dependencies": {
    "@monaco-editor/react": "^4.7.0"
  }
}